# Agent / IDE 模板（复制到自有配置工程）

将本目录内容复制到你的 Luban 配置根（含 `luban.conf` 的目录）：

```text
AGENTS.md                 →  项目根
.cursor/rules/luban.mdc   →  .cursor/rules/luban.mdc
```

然后按项目修改 AGENTS.md 中的目录名、生成命令与分组说明。

官方 Skills 从 Luban 源码仓库 `ai/skills/` 复制到 `.cursor/skills/`。
